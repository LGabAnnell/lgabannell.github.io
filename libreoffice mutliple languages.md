### To install language packs for libreoffice on fedora

run `# dnf install libreoffice-langpack-<language code>`. If unsure about the language code, run `$ dnf search libreoffice-langpack-*` to see the list of available language packs.

Unfortunately required a reboot for me to work.
